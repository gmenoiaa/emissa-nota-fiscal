# Local development: use the certificate path.
NFSE_CERT_PATH=./certificates/certificate.pfx
# Vercel: use the base64 content instead of a filesystem path.
NFSE_CERT_BASE64=
NFSE_CERT_PASSWORD=

# Required on Vercel. Use long, unique random values.
APP_PASSWORD=
AUTH_SECRET=

# Restricted production is the default until you explicitly enable production.
NFSE_ENV=restricted
NFSE_RESTRICTED_URL=https://sefin.producaorestrita.nfse.gov.br/API/SefinNacional
NFSE_PRODUCTION_URL=https://sefin.nfse.gov.br/SefinNacional
NFSE_ALLOW_PRODUCTION=false

# Series 1 is reserved for this own application and starts with DPS 1.
NFSE_NEXT_DPS_NUMBER=1

# Required on Vercel for atomic DPS numbering. An Upstash integration can create these.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Keep the series in the key so it does not reuse the old Web Issuer sequence.
NFSE_DPS_SEQUENCE_KEY=nfse:28220610000110:00001:next-dps-number

# Invoices comerciais. A numeração começa em INV-1038 (INV-1037 foi a última
# emitida fora desta aplicação).
INVOICE_NEXT_NUMBER=1038
INVOICE_SEQUENCE_KEY=invoice:28220610000110:next-number

# Envio por e-mail via Resend. Sem INVOICE_FROM_EMAIL a aplicação usa o remetente
# compartilhado de teste do Resend, que só entrega para INVOICE_TEST_RECIPIENT —
# o destinatário real do cliente é ignorado e citado no corpo da mensagem.
RESEND_API_KEY=
INVOICE_FROM_EMAIL=GWM Informatica <billing@gwminfo.net>
INVOICE_REPLY_TO=gmenoiaa@gmail.com
# Em modo teste o Resend só entrega para o e-mail dono da conta.
INVOICE_TEST_RECIPIENT=gmenoiaa@gmail.com
INVOICE_BCC=
